<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/search_nav_graph"
    app:startDestination="@id/searchFragment">

    <fragment
        android:id="@+id/searchFragment"
        android:name="com.example.project_myfit.searchActivity.SearchFragment"
        android:label="SearchFragment"
        tools:layout="@layout/fragment_search">
        <action
            android:id="@+id/action_searchFragment_to_selectedItemDeleteDialog2"
            app:destination="@id/selectedItemDeleteDialog2" />
        <action
            android:id="@+id/action_searchFragment_to_treeViewDialog2"
            app:destination="@id/treeViewDialog2" />
        <action
            android:id="@+id/action_searchFragment_to_addDialog2"
            app:destination="@id/addDialog2" />
    </fragment>
    <dialog
        android:id="@+id/addDialog2"
        android:name="com.example.project_myfit.dialog.AddDialog"
        android:label="AddDialog">
        <argument
            android:name="item_type"
            app:argType="string" />
        <argument
            android:name="parentCategory"
            app:argType="string" />
        <argument
            android:name="folderId"
            app:argType="long" />
        <action
            android:id="@+id/action_addDialog2_to_sameNameAddDialog2"
            app:destination="@id/sameNameAddDialog2" />
    </dialog>
    <dialog
        android:id="@+id/selectedItemDeleteDialog2"
        android:name="com.example.project_myfit.dialog.SelectedItemDeleteDialog"
        android:label="SelectedItemDeleteDialog">
        <argument
            android:name="selected_item_size"
            app:argType="integer" />
    </dialog>
    <dialog
        android:id="@+id/treeViewDialog2"
        android:name="com.example.project_myfit.dialog.TreeViewDialog"
        android:label="TreeViewDialog">
        <argument
            android:name="parent_category"
            app:argType="string"
            app:nullable="false" />
        <action
            android:id="@+id/action_treeViewDialog2_to_addDialog2"
            app:destination="@id/addDialog2" />
        <action
            android:id="@+id/action_treeViewDialog2_to_itemMoveDialog2"
            app:destination="@id/itemMoveDialog2" />
    </dialog>
    <dialog
        android:id="@+id/itemMoveDialog2"
        android:name="com.example.project_myfit.dialog.ItemMoveDialog"
        android:label="ItemMoveDialog">
        <argument
            android:name="selected_item_size"
            app:argType="integer" />
        <argument
            android:name="folder_id"
            app:argType="long" />
    </dialog>
    <dialog
        android:id="@+id/sameNameAddDialog2"
        android:name="com.example.project_myfit.dialog.SameNameAddDialog"
        android:label="SameNameAddDialog">
        <argument
            android:name="item_type"
            app:argType="string" />
        <argument
            android:name="parent_category"
            app:argType="string" />
        <argument
            android:name="folder_id"
            app:argType="long" />
        <argument
            android:name="new_name"
            app:argType="string" />
    </dialog>
</navigation>