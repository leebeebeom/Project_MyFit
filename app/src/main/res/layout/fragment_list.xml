<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="listFragment"
            type="com.example.myfit.ui.main.list.ListFragment" />

        <variable
            name="model"
            type="com.example.myfit.ui.main.list.ListViewModel" />
    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?android:colorBackground"
        tools:context=".ui.main.list.ListFragment">

        <com.example.myfit.util.LockableScrollView
            android:id="@+id/sv"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fillViewport="true">

            <androidx.appcompat.widget.LinearLayoutCompat
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical">

                <include
                    android:id="@+id/textNavigation"
                    layout="@layout/fragment_list_text_navigation"
                    app:listFragment="@{listFragment}"
                    app:model="@{model}" />

                <include
                    android:id="@+id/rvFolder"
                    layout="@layout/fragment_list_recycler_view_folder"
                    app:listFragment="@{listFragment}"
                    app:model="@{model}" />

                <include
                    android:id="@+id/rvSize"
                    layout="@layout/fragment_list_recycler_view_size"
                    app:listFragment="@{listFragment}"
                    app:model="@{model}" />
            </androidx.appcompat.widget.LinearLayoutCompat>
        </com.example.myfit.util.LockableScrollView>
    </androidx.coordinatorlayout.widget.CoordinatorLayout>
</layout>
